# Node
node_modules/
npm-debug.log*

# OS / editor
Thumbs.db
desktop.ini
.DS_Store
.vscode/

# Local archives — never publish contaminated/raw backups
*.CONTAMINATED*.bak
*.bak

# Local planning notes — this repo is public; internal planning stays on the VPS.
WAITLIST_SETUP.md
